# 个人记事本 + 日历应用 — 开发与部署方案

> 技术栈：Vue 3 + Vite + Vant 4 + TypeScript + Supabase + Cloudflare Pages
> 目标：免费、可公网访问、PC + H5 兼容、支持多设备云同步、无自有域名

---

## 目录

1. [项目概述](#1-项目概述)
2. [技术栈选型与理由](#2-技术栈选型与理由)
3. [系统架构](#3-系统架构)
4. [数据库设计（Supabase / PostgreSQL）](#4-数据库设计supabase--postgresql)
5. [功能模块详细设计](#5-功能模块详细设计)
6. [云同步策略](#6-云同步策略)
7. [项目目录结构](#7-项目目录结构)
8. [开发环境与依赖清单](#8-开发环境与依赖清单)
9. [开发实施计划](#9-开发实施计划)
10. [部署方案（Cloudflare Pages + Supabase）](#10-部署方案cloudflare-pages--supabase)
11. [成本估算](#11-成本估算)
12. [安全与隐私](#12-安全与隐私)
13. [后续升级路径](#13-后续升级路径)
14. [附录：关键代码片段](#14-附录关键代码片段)
15. [实际部署与排障记录](#15-实际部署与排障记录2026-08-06)

---

## 1. 项目概述

### 1.1 产品定位

一个个人使用的轻量级记事本 + 日历应用，支持：

- **记事本**：Markdown 编辑 + 实时预览，标签分类，全文搜索，置顶
- **日历**：月视图，日期关联笔记/事件，事件提醒（浏览器通知）
- **多设备云同步**：登录后数据云端备份，跨设备同步
- **PWA**：可安装到手机桌面，离线可查看
- **响应式**：PC 宽屏 + 手机 H5 自适应

### 1.2 核心约束

| 约束 | 说明 |
|------|------|
| 无自有域名 | 使用平台免费子域名（`*.pages.dev`） |
| 免费优先 | 所有服务使用免费额度 |
| 公网可访问 | 任何人通过 URL 可访问（登录后用） |
| PC + H5 兼容 | 响应式布局，移动端优先 |
| 多设备同步 | 需要云后端支持 |

---

## 2. 技术栈选型与理由

### 2.1 前端

| 技术 | 版本 | 作用 | 选型理由 |
|------|------|------|---------|
| Vue 3 | ^3.4 | 核心框架 | 轻量、组合式 API、生态成熟 |
| Vite | ^5.x | 构建工具 | 极速 HMR、打包产物小、Cloudflare 构建快 |
| Vant | ^4.8 | H5 移动端组件库 | 日历/弹层/表单/Toast 开箱即用，移动端体验最佳 |
| TypeScript | ^5.4 | 类型系统 | 重构不踩坑，IDE 智能提示 |
| Vue Router | ^4.3 | 路由 | 多页面切换 |
| Pinia | ^2.1 | 状态管理 | Vue 3 官方推荐，比 Vuex 简单 |
| Dexie.js | ^4.0 | IndexedDB 封装 | 离线缓存层，让 IndexedDB 操作简化 |
| md-editor-v3 | ^4.x | Markdown 编辑器 | Vue 3 原生支持，编辑+预览+工具栏齐全 |
| @supabase/supabase-js | ^2.4 | Supabase 客户端 | 认证 + 数据库 + 实时订阅 |
| vite-plugin-pwa | ^0.20 | PWA 插件 | Service Worker + manifest，可安装到桌面 |
| dayjs | ^1.11 | 日期处理 | 轻量替代 moment |

### 2.2 后端（BaaS）

| 服务 | 作用 | 选型理由 |
|------|------|---------|
| **Supabase** | PostgreSQL 数据库 + Auth + Realtime | 免费额度大（500MB DB、50000 月活、1GB 存储），开源可自托管，PostgreSQL 功能强大，自带行级安全（RLS） |

**为什么不用 Firebase？** Firebase 免费额度也够，但用 Firestore（NoSQL），数据迁移和复杂查询不如 PostgreSQL 灵活；且 Firebase 国内访问不稳定。Supabase 同样是海外服务，但 PostgreSQL + REST/Realtime 接口对国内访问相对友好，且后续可自托管。

### 2.3 部署

| 平台 | 作用 | 选型理由 |
|------|------|---------|
| **Cloudflare Pages** | 静态站点托管 | 无限请求、500 次构建/月、20GB 带宽、自带 `*.pages.dev` 子域名 + HTTPS、全球 CDN、国内访问在免费平台里最稳定 |

---

## 3. 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                      用户设备（浏览器）                       │
│  ┌───────────────────────────────────────────────────────┐  │
│  │            Vue 3 SPA（PC + H5 响应式）                │  │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌─────────┐ │  │
│  │  │ 记事本    │ │ 日历      │ │ 设置      │ │ 登录注册 │ │  │
│  │  │ 模块      │ │ 模块      │ │ 模块      │ │ 模块    │ │  │
│  │  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬────┘ │  │
│  │       └────────────┼────────────┼────────────┘      │  │
│  │              Pinia 状态管理 + 同步引擎               │  │
│  │       ┌────────────┼────────────────┐               │  │
│  │       ▼                              ▼               │  │
│  │  IndexedDB                       Supabase Client     │  │
│  │  （Dexie.js）                     （@supabase/js）    │  │
│  │  离线缓存层                       云端同步层          │  │
│  └───────────────┬───────────────────────┬─────────────┘  │
│                  │                       │                 │
│           Service Worker                 │                 │
│          （PWA 离线）                     │                 │
└──────────────────┼───────────────────────┼─────────────────┘
                   │                       │ HTTPS
                   │                       ▼
                   │              ┌─────────────────┐
                   │              │   Supabase 云    │
                   │              │  ┌────────────┐  │
                   │              │  │ PostgreSQL │  │
                   │              │  │  + RLS     │  │
                   │              │  └────────────┘  │
                   │              │  ┌────────────┐  │
                   │              │  │ GoTrue    │  │
                   │              │  │  Auth     │  │
                   │              │  └────────────┘  │
                   │              │  ┌────────────┐  │
                   │              │  │ Realtime   │  │
                   │              │  │  订阅      │  │
                   │              │  └────────────┘  │
                   │              └─────────────────┘
                   ▼
           ┌─────────────────┐
           │ Cloudflare Pages │
           │  静态资源 CDN     │
           │  *.pages.dev     │
           └─────────────────┘
```

### 3.1 数据流说明

1. **写入流程**：用户编辑笔记 → 先写入 IndexedDB（立即响应）→ 后台异步同步到 Supabase → 成功后更新本地同步状态
2. **读取流程**：优先从 IndexedDB 读取（离线可用）→ 联网时拉取云端增量更新
3. **实时同步**：通过 Supabase Realtime 订阅表变更，多设备编辑时实时推送
4. **离线处理**：断网时所有操作落在 IndexedDB，联网后自动补传

---

## 4. 数据库设计（Supabase / PostgreSQL）

> 本节全部 SQL 已整理为可一键执行的脚本 `supabase/schema.sql`（含建表、索引、RLS、触发器、Realtime 启用，幂等可重复执行），在 Supabase SQL Editor 中直接运行即可。

### 4.1 表结构

#### `notes` 笔记表

```sql
create table notes (
  id uuid primary key default gen_random_uuid(),
  user_id uuid references auth.users(id) on delete cascade not null,
  title text not null default '',
  content text not null default '',          -- Markdown 原文
  tags text[] default '{}',                  -- 标签数组
  note_date date,                            -- 关联的日历日期（可空）
  is_pinned boolean default false,           -- 是否置顶
  is_deleted boolean default false,          -- 软删除标记
  created_at timestamptz default now(),
  updated_at timestamptz default now(),
  -- 同步冲突用：客户端最后更新时间戳（毫秒）
  client_updated_at bigint not null default (extract(epoch from now()) * 1000)::bigint
);

-- 索引
create index idx_notes_user_id on notes(user_id);
create index idx_notes_user_date on notes(user_id, note_date);
create index idx_notes_updated_at on notes(user_id, updated_at);
```

#### `events` 日历事件表

```sql
create table events (
  id uuid primary key default gen_random_uuid(),
  user_id uuid references auth.users(id) on delete cascade not null,
  title text not null,
  description text default '',
  event_date date not null,
  event_time time,                           -- 具体时间（可空，空为全天）
  remind_minutes int,                        -- 提前提醒分钟数（可空）
  is_deleted boolean default false,
  created_at timestamptz default now(),
  updated_at timestamptz default now(),
  client_updated_at bigint not null default (extract(epoch from now()) * 1000)::bigint
);

create index idx_events_user_date on events(user_id, event_date);
```

#### `sync_state` 同步状态表（可选，记录最后同步时间）

```sql
create table sync_state (
  user_id uuid primary key references auth.users(id) on delete cascade,
  last_sync_at timestamptz default now()
);
```

### 4.2 行级安全策略（RLS）

> 关键：确保用户只能访问自己的数据

```sql
-- notes 表 RLS
alter table notes enable row level security;

create policy "用户可查自己的笔记"
  on notes for select using (auth.uid() = user_id);

create policy "用户可增自己的笔记"
  on notes for insert with check (auth.uid() = user_id);

create policy "用户可改自己的笔记"
  on notes for update using (auth.uid() = user_id);

create policy "用户可删自己的笔记"
  on notes for delete using (auth.uid() = user_id);

-- events 表同理
alter table events enable row level security;

create policy "用户可查自己的事件"
  on events for select using (auth.uid() = user_id);

create policy "用户可增自己的事件"
  on events for insert with check (auth.uid() = user_id);

create policy "用户可改自己的事件"
  on events for update using (auth.uid() = user_id);

create policy "用户可删自己的事件"
  on events for delete using (auth.uid() = user_id);
```

### 4.3 触发器：自动更新 updated_at

```sql
create or replace function update_updated_at()
returns trigger as $$
begin
  new.updated_at = now();
  return new;
end;
$$ language plpgsql;

create trigger trg_notes_updated_at
  before update on notes
  for each row execute function update_updated_at();

create trigger trg_events_updated_at
  before update on events
  for each row execute function update_updated_at();
```

---

## 5. 功能模块详细设计

### 5.1 记事本模块

| 功能 | 说明 | 技术实现 |
|------|------|---------|
| 笔记列表 | 按更新时间倒序，置顶优先 | Dexie 查询 + 虚拟滚动（量大时） |
| 新建笔记 | 标题 + Markdown 正文 + 标签 + 日期 | md-editor-v3 编辑器 |
| 编辑笔记 | 自动保存（防抖 2s） | watch + debounce |
| 删除笔记 | 软删除（移入回收站） | `is_deleted = true` |
| 搜索 | 标题 + 正文全文搜索 | IndexedDB 索引 + 内存过滤 |
| 标签管理 | 多标签、按标签筛选 | tags 数组字段 |
| 置顶 | 单条置顶切换 | `is_pinned` 字段 |
| Markdown 预览 | 编辑时实时预览 | md-editor-v3 内置 |
| 导出 | 单条/全部导出为 JSON / Markdown | Blob + a.download |

### 5.2 日历模块

| 功能 | 说明 | 技术实现 |
|------|------|---------|
| 月视图 | 显示当月，有笔记/事件的日期标记 | Vant Calendar 组件 + 自定义底部 |
| 日期点击 | 展开当天笔记列表 + 事件列表 | 弹层展示 |
| 新建事件 | 标题 + 日期 + 时间 + 提醒 | Vant Popup + Form |
| 事件提醒 | 到点浏览器通知 | Service Worker + Notification API |
| 农历显示 | 日历格子显示农历 | lunar-javascript 库 |
| 跳转笔记 | 从日历快速新建当天笔记 | 路由跳转带日期参数 |

### 5.3 用户与同步模块

| 功能 | 说明 | 技术实现 |
|------|------|---------|
| 注册/登录 | 邮箱密码 + 魔法链接 | Supabase Auth |
| 退出登录 | 清除会话 | supabase.auth.signOut() |
| 首次同步 | 登录后拉取全量数据到本地 | 增量查询 by updated_at |
| 增量同步 | 仅同步变更数据 | client_updated_at 时间戳比对 |
| 实时同步 | 多设备编辑实时推送 | Supabase Realtime 订阅 |
| 冲突处理 | 以 client_updated_at 大的为准 | Last-Write-Wins 策略 |
| 离线缓存 | 断网仍可读写 | IndexedDB 为主存储 |

### 5.4 设置模块

| 功能 | 说明 |
|------|------|
| 主题切换 | 明 / 暗 / 跟随系统 |
| 数据导出 | 全量 JSON 备份 |
| 数据导入 | JSON 恢复 |
| 同步状态 | 显示最后同步时间、手动触发同步 |
| 账号管理 | 退出登录、修改密码 |

---

## 6. 云同步策略

### 6.1 同步模型：Local-First（本地优先）

```
用户操作 ──→ 写入 IndexedDB（立即返回）
                │
                ▼
          同步队列（待推送）
                │ 联网？
                ▼
          推送到 Supabase
                │
                ▼
          更新本地同步状态
```

**优势**：
- 写入零延迟（不等网络）
- 离线完全可用
- 网络恢复后自动补传

### 6.2 同步流程

#### 6.2.1 登录后全量拉取

**实际实现采用「全量拉取 + 逐行 LWW 合并」**（个人应用数据量小，每次同步拉取当前用户全部数据）：

```typescript
async function fullSync() {
  await pushPendingChanges();   // 1. 先推送本地待同步变更（同步队列）
  await pullRemoteChanges();    // 2. 再全量拉取云端数据
}

async function pullRemoteChanges() {
  // 拉取当前用户全部笔记/事件（不做 updated_at 增量过滤）
  const remoteNotes = await supabase.from('notes').select('*').eq('user_id', userId);
  for (const rn of remoteNotes) {
    const local = await db.notes.get(rn.id);
    // 本地不存在，或云端 client_updated_at 更大 → 覆盖本地
    if (!local || rn.client_updated_at > local.client_updated_at) {
      await db.notes.put({ ...rn, synced: true });
    }
  }
  // 对账：本地已同步但云端不存在的记录（其他设备硬删）→ 移除本地
  // 本地有未推送变更（syncQueue 有待办项）的记录 → 保留
}
```

> **为什么不用增量拉取（`updated_at > last_pull_at`）？**
> 实际排障中发现：增量过滤会漏掉「更新时间早于上次拉取时间但本地从未见过」的行；
> 且云端对账逻辑若基于增量结果，会把云端暂时查不到、但实际存在的本地已同步数据误删（数据丢失）。
> 个人应用单用户数据量小（数百条内），全量拉取简单且正确。

触发时机：

- 登录成功 / 会话恢复后自动同步一次
- 应用启动时（已登录状态）
- 网络恢复（`online` 事件）自动补同步
- 设置页"立即同步"按钮手动触发
- 每次本地写入后自动推送待同步变更

#### 6.2.2 实时订阅

```typescript
// 多设备实时同步
supabase
  .channel('notes-changes')
  .on('postgres_changes', 
    { event: '*', schema: 'public', table: 'notes' },
    (payload) => {
      // 收到其他设备的变更，更新本地
      handleRemoteChange(payload);
    }
  )
  .subscribe();
```

### 6.3 冲突解决：Last-Write-Wins

- 每条记录有 `client_updated_at`（客户端毫秒时间戳）
- 同步时比对：云端 vs 本地，时间戳大的覆盖小的
- 极端情况（同一毫秒并发编辑）概率极低，可接受

### 6.4 未登录模式

- 用户可不注册直接使用（本地模式）
- 数据仅存 IndexedDB
- 登录后才触发云同步

---

## 7. 项目目录结构

```
notes-calendar-app/
├── public/
│   ├── favicon.ico
│   └── icons/                    # PWA 图标
├── src/
│   ├── assets/                   # 静态资源
│   ├── components/               # 通用组件
│   │   ├── NoteEditor.vue        # Markdown 编辑器封装
│   │   ├── NoteList.vue          # 笔记列表
│   │   ├── CalendarView.vue      # 日历视图
│   │   ├── EventForm.vue         # 事件表单
│   │   └── SearchBar.vue         # 搜索栏
│   ├── views/                    # 页面
│   │   ├── HomeView.vue          # 首页（笔记列表）
│   │   ├── NoteEditView.vue      # 笔记编辑页
│   │   ├── CalendarView.vue      # 日历页
│   │   ├── SettingsView.vue      # 设置页
│   │   └── AuthView.vue          # 登录注册页
│   ├── stores/                   # Pinia 状态
│   │   ├── auth.ts               # 用户认证
│   │   ├── notes.ts              # 笔记状态
│   │   ├── events.ts             # 事件状态
│   │   └── settings.ts           # 设置（主题等）
│   ├── services/                 # 服务层
│   │   ├── supabase.ts           # Supabase 客户端
│   │   ├── db.ts                 # Dexie 本地数据库
│   │   ├── sync.ts               # 同步引擎
│   │   └── notification.ts       # 提醒服务
│   ├── composables/              # 组合式函数
│   │   ├── useTheme.ts           # 主题切换
│   │   └── useSync.ts            # 同步状态
│   ├── router/
│   │   └── index.ts
│   ├── utils/
│   │   ├── markdown.ts           # Markdown 工具
│   │   └── date.ts               # 日期工具
│   ├── types/
│   │   └── index.ts              # TypeScript 类型
│   ├── App.vue
│   └── main.ts
├── .env.example                  # 环境变量模板
├── .gitignore
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
└── README.md
```

---

## 8. 开发环境与依赖清单

### 8.1 环境要求

- Node.js >= 18（推荐 20 LTS）
- npm 或 pnpm（推荐 pnpm，更快）

### 8.2 package.json 关键依赖

```json
{
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "^4.3.0",
    "pinia": "^2.1.0",
    "vant": "^4.8.0",
    "@vant/use": "^1.6.0",
    "dexie": "^4.0.0",
    "@supabase/supabase-js": "^2.4.0",
    "md-editor-v3": "^4.12.0",
    "dayjs": "^1.11.0",
    "lunar-javascript": "^1.6.0"
  },
  "devDependencies": {
    "vite": "^5.2.0",
    "@vitejs/plugin-vue": "^5.0.0",
    "typescript": "^5.4.0",
    "vue-tsc": "^2.0.0",
    "vite-plugin-pwa": "^0.20.0",
    "unplugin-vue-components": "^0.26.0",
    "@vant/auto-import-resolver": "^1.2.0"
  }
}
```

### 8.3 环境变量

`.env.example`（模板，占位符保留，可提交到仓库）：

```
VITE_SUPABASE_URL=https://你的项目.supabase.co
VITE_SUPABASE_ANON_KEY=你的anon公钥
```

`.env`（实际配置，已加入 .gitignore，**不可提交**）：

```
VITE_SUPABASE_URL=https://jkbmuertlelpqchhuhzi.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_iks9-8iGi1hFdRlAirC0uA_SspTMkcg
```

使用步骤：

```bash
cp .env.example .env   # 复制一份后编辑真实值
```

> **Key 类型说明（2026 新版）**：Supabase 已用新的 "Publishable / Secret API keys" 取代旧的 "anon / service_role keys"。
> - 前端必须使用 **Publishable key**（对应旧 anon key），格式 `sb_publishable_xxx`，是公开密钥，安全由 RLS 保证，可放前端
> - **Secret key**（对应旧 service_role）权限极高且绕过 RLS，**严禁放入前端或仓库**，仅服务端使用
> - 旧版 anon/service_role key 官方计划 2026 年底移除，新项目直接用新版

---

## 9. 开发实施计划

### 阶段一：项目初始化与基础框架（0.5 天）

- [ ] Vite + Vue 3 + TS 脚手架
- [ ] 集成 Vant、Vue Router、Pinia
- [ ] 配置 vite-plugin-pwa
- [ ] 响应式布局骨架（移动端 Tabbar + PC 侧边栏）
- [ ] 明暗主题切换

### 阶段二：本地数据层与记事本（1 天）

- [ ] Dexie.js 封装本地数据库
- [ ] 笔记 CRUD
- [ ] md-editor-v3 集成
- [ ] 笔记列表、搜索、标签、置顶
- [ ] 自动保存（防抖）

### 阶段三：日历模块（0.5 天）

- [ ] Vant Calendar 月视图
- [ ] 日期关联笔记/事件展示
- [ ] 事件 CRUD
- [ ] 农历显示
- [ ] 事件提醒（Notification API）

### 阶段四：Supabase 后端搭建（0.5 天）

- [ ] 注册 Supabase 账号，创建项目
- [ ] 执行 SQL 建表 + RLS + 触发器
- [ ] 配置 Auth（邮箱登录）
- [ ] 获取 URL 和 anon key

### 阶段五：云同步集成（1 天）

- [ ] Supabase 客户端封装
- [ ] 登录/注册页面
- [ ] 同步引擎（local-first）
- [ ] 增量同步 + Realtime 订阅
- [ ] 冲突处理
- [ ] 同步状态展示

### 阶段六：设置与数据管理（0.5 天）

- [ ] 主题切换完善
- [ ] JSON 导入导出
- [ ] 回收站
- [ ] PWA 安装提示

### 阶段七：测试与部署（0.5 天）

- [ ] 本地构建测试
- [ ] 移动端真机测试
- [ ] 推送 GitHub
- [ ] Cloudflare Pages 部署
- [ ] 验证公网访问

**预计总工时：4.5 天**（熟手可压缩到 3 天）

---

## 10. 部署方案（Cloudflare Pages + Supabase）

### 10.1 Supabase 配置

**本项目实际配置（2026-08-06）：**

- 项目名：`notes-calendar`（显示名称，非地址）
- 区域：Northeast Asia（Tokyo）
- Project URL：`https://jkbmuertlelpqchhuhzi.supabase.co`
- Publishable key：`sb_publishable_iks9-8iGi1hFdRlAirC0uA_SspTMkcg`

操作步骤：

1. 访问 https://supabase.com 注册（可用 GitHub 登录）
2. New Project → 填项目名、数据库密码、选区域（推荐 Northeast Asia - Tokyo，离国内近）
   - 创建页的 **"Enable automatic RLS" 选项建议勾选**：任何新建表自动开启 RLS（安全兜底，与手动建表 SQL 不冲突）
3. 等待 2 分钟初始化
4. 进入 SQL Editor，执行仓库内 `supabase/schema.sql`（建表 + RLS + 触发器 + Realtime 启用，幂等可重复执行）
5. 进入 Authentication → Sign In / Providers → 启用 **Email**
   - 测试期在 Authentication → Settings 关闭 **"Confirm email"**（否则注册后需先邮件确认；注意：关闭设置不会自动确认已有用户，可用"注册"页重新注册新账号）
6. 进入 Settings → API keys，选择 **"Publishable and secret API keys"** 页签，复制：
   - **Publishable key**（`sb_publishable_...`，放前端用这个）
   - ⚠️ Secret key 仅在服务端场景使用，不要复制到前端
7. 填入项目 `.env` 文件（见 8.3）

### 10.2 Cloudflare Pages 部署

**本项目实际配置（2026-08-06）：**

- 线上地址：`https://lihf.pages.dev`（项目名 `lihf`）
- 仓库：`https://github.com/lihf2012/notes-calendar`（Git 自动部署）

#### 方式一：Git 自动部署（推荐，本项目采用）

1. 代码推送到 GitHub 仓库
2. 注册 https://dash.cloudflare.com（免费）
3. 左侧 Workers & Pages → Create → Pages → Connect to Git
4. 授权 GitHub，选择仓库
5. 配置：
   - **Framework preset**：Vue
   - **Build command**：`npm run build`
   - **Build output directory**：`dist`
6. **Environment variables（必须配置，构建时注入）：**

   | 变量 | 值 |
   |------|-----|
   | `NODE_VERSION` | `20` |
   | `VITE_SUPABASE_URL` | `https://jkbmuertlelpqchhuhzi.supabase.co` |
   | `VITE_SUPABASE_ANON_KEY` | `sb_publishable_iks9-8iGi1hFdRlAirC0uA_SspTMkcg` |

7. Save and Deploy，等待 1~2 分钟构建完成
8. 获得 `https://项目名.pages.dev` 公网地址（本项目为 `https://lihf.pages.dev`）

> **⚠️ 关键注意事项**
> - 环境变量是**构建时**注入前端 bundle 的，必须在触发构建**之前**在 Cloudflare 控制台配好
> - 环境变量**不进 Git 仓库**（`.env` 已被 gitignore），push 不会携带 key；不配变量线上会退回纯本地模式（登录页显示"云服务未配置"）
> - 每次 `git push` Cloudflare 自动重新构建部署；改完环境变量后需在 Deployments 页手动触发一次重新构建
> - 验证配置是否生效：打开 `https://lihf.pages.dev/auth`，页面应显示"登录开启多设备云同步"（而不是"云服务未配置"）

#### 方式二：Wrangler CLI 手动部署

```bash
npm install -g wrangler
wrangler login
npm run build
wrangler pages deploy dist --project-name=notes-calendar
```

### 10.3 移动端使用

1. 手机浏览器打开 `https://项目名.pages.dev`
2. 浏览器菜单 → "添加到主屏幕"
3. 桌面生成应用图标，点击全屏启动，体验接近原生 App

### 10.4 自定义子域名（可选）

Cloudflare Pages 默认子域名是随机的，可在项目设置里改成自定义的：
- Pages → 项目 → Custom domains → Set up
- 可用 `你的名字.pages.dev`（未被占用的话）

---

## 11. 成本估算

| 服务 | 免费额度 | 本项目预计用量 | 是否超免费 |
|------|---------|---------------|-----------|
| Cloudflare Pages | 无限请求、500 构建/月、20GB 带宽 | 个人用远低于 | ✅ 免费 |
| Supabase | 500MB DB、1GB 存储、50000 月活、2GB 带宽 | 个人用远低于 | ✅ 免费 |
| GitHub | 公开仓库无限 | 1 个仓库 | ✅ 免费 |
| **合计** | | | **¥0/月** |

> 仅当用户量或数据量暴涨时才会触发付费，个人使用基本永远免费。

---

## 12. 安全与隐私

### 12.1 数据安全

- **RLS 行级安全**：数据库层面保证用户只能访问自己的数据，即使 anon key 泄露也无法越权
- **HTTPS 全链路**：Cloudflare 和 Supabase 默认 HTTPS
- **软删除**：笔记删除先进回收站，可恢复

### 12.2 隐私说明

- 笔记内容存储在 Supabase（海外节点），理论上有数据跨境合规问题
- 若介意，可后续自托管 Supabase（开源）部署在国内服务器
- 本地 IndexedDB 始终有完整副本，云端只是备份 + 同步

### 12.3 认证安全

- 使用 Supabase Auth（基于 JWT）
- 支持邮箱密码 + 魔法链接
- 可后续接入 GitHub/Google OAuth

---

## 13. 后续升级路径

| 升级项 | 说明 | 触发条件 |
|--------|------|---------|
| 自定义域名 | 买个 10 元/年域名绑 Cloudflare | 想要品牌化地址 |
| 服务端推送提醒 | Cloudflare Workers + Web Push | 需要后台也能收提醒 |
| 富文本编辑 | 替换 md-editor 为 Tiptap | 不想写 Markdown |
| 多端客户端 | 用 Capacitor 打包成 iOS/Android App | 想上架应用商店 |
| 自托管后端 | Supabase 自托管到国内云服务器 | 数据合规要求 |
| 端到端加密 | 客户端加密后再上传 | 极高隐私要求 |

---

## 14. 附录：关键代码片段

### 14.1 Supabase 客户端封装

```typescript
// src/services/supabase.ts
import { createClient } from '@supabase/supabase-js';

const supabaseUrl = import.meta.env.VITE_SUPABASE_URL;
const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY;

export const supabase = createClient(supabaseUrl, supabaseAnonKey, {
  auth: {
    persistSession: true,
    autoRefreshToken: true,
  },
});
```

### 14.2 Dexie 本地数据库

```typescript
// src/services/db.ts
import Dexie, { Table } from 'dexie';

export interface Note {
  id: string;
  title: string;
  content: string;
  tags: string[];
  note_date: string | null;
  is_pinned: boolean;
  is_deleted: boolean;
  created_at: string;
  updated_at: string;
  client_updated_at: number;
  synced: boolean; // 是否已同步到云端
}

export interface EventItem {
  id: string;
  title: string;
  description: string;
  event_date: string;
  event_time: string | null;
  remind_minutes: number | null;
  is_deleted: boolean;
  created_at: string;
  updated_at: string;
  client_updated_at: number;
  synced: boolean;
}

class AppDB extends Dexie {
  notes!: Table<Note>;
  events!: Table<EventItem>;
  syncQueue!: Table<{ id: string; table: string; operation: string; data: any }>;

  constructor() {
    super('NotesCalendarDB');
    this.version(1).stores({
      notes: 'id, is_deleted, is_pinned, note_date, client_updated_at, *tags',
      events: 'id, is_deleted, event_date, client_updated_at',
      syncQueue: 'id, table',
    });
  }
}

export const db = new AppDB();
```

### 14.3 同步引擎核心

```typescript
// src/services/sync.ts
import { supabase, isSupabaseConfigured } from './supabase';
import { db } from './db';

type TableName = 'notes' | 'events';

// 本地字段 → 云端字段：剔除本地专用字段（synced）
// ⚠️ 关键：payload 若携带 synced 字段，云端无此列，upsert 会报错
function toCloudPayload(tableName: TableName, record: any): Record<string, unknown> {
  const base = {
    id: record.id,
    title: record.title,
    content: record.content,
    tags: record.tags,
    note_date: record.note_date,
    is_pinned: record.is_pinned,
    is_deleted: record.is_deleted,
    created_at: record.created_at,
    updated_at: record.updated_at,
    client_updated_at: record.client_updated_at,
  };
  if (tableName === 'notes') return base;
  return { ...base, description: record.description, event_date: record.event_date,
    event_time: record.event_time, remind_minutes: record.remind_minutes };
}

export async function pushPendingChanges() {
  const pending = await db.syncQueue.toArray();
  for (const item of pending) {
    try {
      if (item.operation === 'delete') {
        // 硬删除：先删云端记录
        await supabase.from(item.table_name).delete().eq('id', item.record_id);
      } else {
        const payload = { ...toCloudPayload(item.table_name, item.payload), user_id: userId };
        await supabase.from(item.table_name).upsert(payload);
      }
      await db.syncQueue.delete(item.id);
      await db.table(item.table_name).update(item.record_id, { synced: true });
    } catch (e) {
      console.warn('同步失败，待重试', e); // 保留在队列，下次重试
    }
  }
}

export async function pullRemoteChanges() {
  // 全量拉取 + 逐行 client_updated_at 比对（LWW），不做增量过滤
  const { data: remoteNotes } = await supabase
    .from('notes').select('*').eq('user_id', userId);
  for (const rn of remoteNotes ?? []) {
    const local = await db.notes.get(rn.id);
    if (!local || (rn.client_updated_at ?? 0) > local.client_updated_at) {
      await db.notes.put({ ...rn, synced: true });
    }
  }
  // 对账：本地已同步（synced=true、user_id 匹配、无待推送）但云端不存在的记录 → 删除本地
}

export async function fullSync() {
  await pushPendingChanges();
  await pullRemoteChanges();
}
```

> **硬删除（回收站清除）**：`hardDeleteNote/hardDeleteEvent` 在清本地记录的同时，
> 会入队一条 `operation: 'delete'` 的同步项；`enqueueSync` 对同一记录的去重策略是
> 新操作覆盖旧待同步项，避免残留与冲突。

### 14.4 vite.config.ts

```typescript
import { defineConfig } from 'vite';
import vue from '@vitejs/plugin-vue';
import { VitePWA } from 'vite-plugin-pwa';
import Components from 'unplugin-vue-components/vite';
import { VantResolver } from '@vant/auto-import-resolver';

export default defineConfig({
  plugins: [
    vue(),
    Components({ resolvers: [VantResolver()] }),
    VitePWA({
      registerType: 'autoUpdate',
      manifest: {
        name: '记事本日历',
        short_name: '记事本',
        theme_color: '#534AB7',
        icons: [
          {
            src: '/icons/icon-192.png',
            sizes: '192x192',
            type: 'image/png',
          },
          {
            src: '/icons/icon-512.png',
            sizes: '512x512',
            type: 'image/png',
          },
        ],
      },
    }),
  ],
});
```

---

## 15. 实际部署与排障记录（2026-08-06）

### 15.1 账户与资源清单

| 项 | 值 |
|---|---|
| GitHub 仓库 | `https://github.com/lihf2012/notes-calendar` |
| Cloudflare Pages 项目 | 项目名 `lihf`，线上地址 `https://lihf.pages.dev` |
| Supabase 项目 | 项目名 `notes-calendar`，区域 Tokyo |
| Supabase Project URL | `https://jkbmuertlelpqchhuhzi.supabase.co` |
| Supabase Publishable key | `sb_publishable_iks9-8iGi1hFdRlAirC0uA_SspTMkcg` |
| 测试账号 | `notes-calendar-user@outlook.com`（注册页可随时新注册，测试期已关闭邮箱确认） |

### 15.2 已执行的操作

1. **Supabase**：创建项目 → 勾选 "Enable automatic RLS" → SQL Editor 执行 `supabase/schema.sql` → 启用 Email 登录 → 测试期关闭 "Confirm email"
2. **本地**：`cp .env.example .env` 填入真实 URL 与 publishable key（.env 已被 gitignore）
3. **Cloudflare**：项目设置中添加环境变量 `VITE_SUPABASE_URL`、`VITE_SUPABASE_ANON_KEY`、`NODE_VERSION=20`（必须在触发构建前配置，构建时注入）
4. **部署**：`git push` 触发 Cloudflare 自动构建，验证 `https://lihf.pages.dev/auth` 显示"登录开启多设备云同步"

### 15.3 云端同步排障记录（修复清单）

| # | 问题 | 原因 | 修复 |
|---|---|---|---|
| 1 | 推送永远失败 | 本地 payload 携带 `synced` 字段，云端表无此列，PostgREST 报错 | `toCloudPayload()` 显式映射云端字段，剔除本地专属字段 |
| 2 | 回收站硬删除后云端残留、本地复活 | 硬删除只清本地，云端软删除记录会在下次拉取时按 `!local` 复活 | 硬删除入队 `delete` 操作；`enqueueSync` 同记录新操作覆盖旧项 |
| 3 | 魔法链接登录/会话恢复不同步 | `onAuthStateChange` 只更新 user 不触发订阅与全量同步 | 会话变化时调用 `onLoggedIn()`；订阅前先退订旧 channel 防叠加 |
| 4 | 其他设备硬删不消失 | Realtime 忽略 DELETE 事件，离线硬删无对账 | Realtime 处理 DELETE（本地有待推送时保留）；拉取后按完整列表对账 |
| 5 | 登录后列表不刷新 | 登录路径的 `fullSync()` 完成不通知 UI 重载 | 同步完成后派发 `sync:remote-change` 事件触发 store 重载 |
| 6 | 已同步数据被误删 | 增量拉取（`updated_at > last_pull_at`）漏行 + 对账基于增量结果 → 误判云端不存在 | 改为全量拉取 + 逐行 `client_updated_at` LWW 合并，对账基于完整云端列表 |

### 15.4 部署提交记录

```
98385c4 feat: 云同步功能修复与完善
5450234 fix: 登录后全量同步完成时未刷新笔记列表
3b462d2 fix: 全量拉取替代增量拉取，修复对账误删已同步数据
```

---

## 总结

本方案以 **零月费** 实现：

- ✅ PC + H5 双端兼容的记事本 + 日历应用
- ✅ Markdown 编辑 + 实时预览
- ✅ 多设备云同步（Supabase）
- ✅ 公网 HTTPS 访问（Cloudflare Pages `*.pages.dev`）
- ✅ PWA 可安装到手机桌面
- ✅ 离线可用（IndexedDB 本地优先）
- ✅ 数据安全（RLS 行级安全 + JWT 认证）
- ✅ 留好升级口子（自定义域名、推送、App 打包等）

确认无误后，即可按 **第 9 节实施计划** 开始开发。

---

*文档版本：v1.1 | 更新日期：2026-08-06 | 更新内容：写入实际部署配置、密钥、操作记录与排障记录*
